# math-lab 작업 지침

## 기본 정보
- 저장소: joongki-J/math-lab
- 기본 브랜치: main
- 배포 주소: https://joongki-j.github.io/math-lab/
- GitHub Pages 설정: main / (root)

## 홈페이지 유지 원칙
- 기존 ‘중기쌤의 수학 실험실’ 디자인을 유지한다.
- 새 도구를 추가할 때 메인 디자인과 기존 기능을 임의로 변경하지 않는다.
- 수정 후 기존 도구들이 정상 작동하는지 확인한다.

## 새 수학 도구 추가 방법
1. 새 도구를 저장소 최상위의 `<도구명>/index.html`에 저장한다.
2. 메인 `script.js`의 `TOOLS` 배열에 도구 정보를 등록한다.
3. 썸네일은 `thumbnails/` 폴더에 도구 폴더명과 같은 이름으로 저장한다.
4. 과목 분류는 폴더가 아니라 `TOOLS` 배열의 `subject` 값으로 설정한다.
5. 도구의 메인 화면 이동 링크는 `../#tools`를 사용한다.
6. 변경 사항은 `main` 브랜치에 커밋한다.
7. 기존 GitHub Pages 주소에서 정상 반영되었는지 확인한다.

## 작업 방식
- 업로드 전에 변경할 파일과 내용을 사용자에게 설명한다.
- 기존 파일을 삭제하거나 전체 구조를 바꿀 때는 먼저 확인받는다.
- 외부 라이브러리에 의존할 경우 GitHub Pages에서 정상 로드되는지 점검한다.
